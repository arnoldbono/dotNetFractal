<UserControl x:Class="dotNetFractal.WPF.Presentation.ColorMapEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:dotNetFractal.WPF.ViewModels"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Color map bitmap display -->
        <GroupBox Grid.Row="0" Header="Color Gradient Preview" Margin="0,0,0,5">
            <Border BorderBrush="Black" BorderThickness="1" Height="20" Margin="5">
                <Image Source="{Binding ColorMapImage}"
                       RenderOptions.BitmapScalingMode="NearestNeighbor" Stretch="Fill"/>
            </Border>
        </GroupBox>

        <!-- Color table -->
        <GroupBox Grid.Row="1" Header="Color Map Values" Margin="0,0,0,5">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DataGrid Grid.Column="0"
                          ItemsSource="{Binding Colors}" 
                          SelectedItem="{Binding SelectedColor}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          MaxHeight="200">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Red" 
                                            Binding="{Binding Red, UpdateSourceTrigger=PropertyChanged}" 
                                            Width="45"/>
                        <DataGridTextColumn Header="Green" 
                                            Binding="{Binding Green, UpdateSourceTrigger=PropertyChanged}" 
                                            Width="45"/>
                        <DataGridTextColumn Header="Blue" 
                                            Binding="{Binding Blue, UpdateSourceTrigger=PropertyChanged}" 
                                            Width="45"/>
                        <DataGridTextColumn Header="Fraction" 
                                            Binding="{Binding Fraction, UpdateSourceTrigger=PropertyChanged, StringFormat=F4}" 
                                            Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center">
                    <Button Content="Add" 
                            Width="50" 
                            Margin="0,0,0,5"
                            Command="{Binding AddColorCommand}"/>
                    <Button Content="Delete" 
                            Width="50"
                            Margin="0,0,0,5"
                            Command="{Binding DeleteColorCommand}"/>
                    <Button Content="Reset" 
                            Width="50"
                            Command="{Binding ResetColorCommand}"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
