<UserControl x:Class="dotNetFractal.WPF.Presentation.ImageResolutionEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:dotNetFractal.WPF"
             xmlns:converters="clr-namespace:dotNetFractal.WPF.Converters"
             xmlns:models="clr-namespace:dotNetFractal.UI.Models;assembly=dotNetFractal.UI"
             xmlns:ui_vm="clr-namespace:dotNetFractal.UI.ViewModels;assembly=dotNetFractal.UI"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=ui_vm:ImageResolutionViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <converters:EnumBooleanConverter x:Key="EnumBooleanConverterKey"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Header="Select:" Grid.Row="0">
            <StackPanel Margin="0,5,0,0" >
                <RadioButton GroupName="Resolution" Content="Full HD" HorizontalAlignment="Left" VerticalAlignment="Top"
                    IsChecked="{Binding Path=SelectedResolution, Converter={StaticResource EnumBooleanConverterKey}, ConverterParameter={x:Static models:ResolutionEnum.FullHD}}" />
                <RadioButton GroupName="Resolution" Content="4K" HorizontalAlignment="Left" VerticalAlignment="Top"
                    IsChecked="{Binding Path=SelectedResolution, Converter={StaticResource EnumBooleanConverterKey}, ConverterParameter={x:Static models:ResolutionEnum.UltraHD}}" />
                <RadioButton GroupName="Resolution" Content="Custom" HorizontalAlignment="Left" VerticalAlignment="Top"
                IsChecked="{Binding Path=SelectedResolution, Converter={StaticResource EnumBooleanConverterKey}, ConverterParameter={x:Static models:ResolutionEnum.Custom}}" />
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Resolution:" Grid.Row="1" Margin="0,5,0,0">
            <Grid Margin="0,5,0,0" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Content="Width:" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left"/>
                <TextBox Text="{Binding Width}" Width="60" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" HorizontalContentAlignment="Right" VerticalAlignment="Center" IsReadOnly="{Binding ReadOnly}"/>
                <Label Content="Height:" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left"/>
                <TextBox Text="{Binding Height}" Width="60" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" HorizontalContentAlignment="Right" VerticalAlignment="Center" IsReadOnly="{Binding ReadOnly}"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
