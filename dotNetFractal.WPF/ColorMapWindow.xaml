<Window x:Class="dotNetFractal.WPF.ColorMapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:dotNetFractal.WPF"
        mc:Ignorable="d"
        Title="Fractal Color Map" Height="420" Width="450" ResizeMode="CanResize">
    <Window.DataContext>
        <local:ColorMapViewModel />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Color map bitmap display -->
        <GroupBox Grid.Row="0" Header="Color Gradient Preview" Margin="0,0,0,10">
            <Border x:Name="ColorMapBorder" BorderBrush="Black" BorderThickness="1" Height="20" Margin="5">
                <Image x:Name="ColorMap" Source="{Binding ColorMapImage}"
                       RenderOptions.BitmapScalingMode="NearestNeighbor" Stretch="Fill"/>
            </Border>
        </GroupBox>

        <!-- Color table -->
        <GroupBox x:Name="ColorTable" Grid.Row="1" Header="Color Map Values" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DataGrid Grid.Column="0"
                          x:Name="ColorDataGrid"
                          ItemsSource="{Binding Colors}" 
                          SelectedItem="{Binding SelectedColor}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Red" 
                                            Binding="{Binding Red, UpdateSourceTrigger=PropertyChanged}" 
                                            Width="60"/>
                        <DataGridTextColumn Header="Green" 
                                            Binding="{Binding Green, UpdateSourceTrigger=PropertyChanged}" 
                                            Width="60"/>
                        <DataGridTextColumn Header="Blue" 
                                            Binding="{Binding Blue, UpdateSourceTrigger=PropertyChanged}" 
                                            Width="60"/>
                        <DataGridTextColumn Header="Fraction" 
                                            Binding="{Binding Fraction, UpdateSourceTrigger=PropertyChanged, StringFormat=F6}" 
                                            Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center">
                    <Button Content="Add" 
                            Width="60" 
                            Margin="0,0,0,5"
                            Command="{Binding AddColorCommand}"/>
                    <Button Content="Delete" 
                            Width="60"
                            Command="{Binding DeleteColorCommand}"/>
                </StackPanel>
            </Grid>
        </GroupBox>
        
        <!-- OK button -->
        <Button Grid.Row="2" Content="OK" 
                IsDefault="True" 
                Width="60" 
                HorizontalAlignment="Right"
                Command="{Binding AcceptCommand}" 
                CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" />
    </Grid>
</Window>
